# Dependências de DEV/TESTE = produção + ferramentas locais.
# A imagem Docker instala SÓ requirements-prod.txt (sem o que está abaixo).
-r requirements-prod.txt

# Dev / teste (NÃO vão pra imagem de produção)
pytest==7.4.4
pytest-cov==4.1.0
black==24.1.1
flake8==7.0.0
mypy==1.8.0
pre-commit==3.6.0
pgserver==0.1.4  # Postgres efêmero p/ rodar a suíte em PG (scripts/run_tests_postgres.py)
